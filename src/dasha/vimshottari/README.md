# Vimshottari dasha

This module calculates the Vimshottari planetary-period timeline and exposes
queries for the active maha dasha and antar dasha at a date.

Use `calculate` to build the timeline and `at` to query it.
