# Chart internals

This directory contains private chart implementation details shared by the
public chart module.

The constants and position helpers here are not package entry points. Use the
exports from [`../`](../) instead of importing these files directly.
